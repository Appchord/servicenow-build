machine:
  environment:
     SN_ENVIRONMENT:
     SN_USER:
     SN_PWD:
  node:
     version: 6.1.0

test:
  post:
     - if [ -n "${SN_ENVIRONMENT}" ]; then npm run validate-app ${SN_ENVIRONMENT} ${SN_USER} ${SN_PWD}; fi
